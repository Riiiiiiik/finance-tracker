version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile.bot
    image: newsletter-bot
    command: python3 -u scripts/scheduler.py
    restart: always
    env_file:
      - .env
    volumes:
      - ./scripts:/app/scripts
