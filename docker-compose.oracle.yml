version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile.bot
    image: newsletter-bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./scripts:/app/scripts
